apiVersion: dra.example.com/v1
kind: DeviceClass
metadata:
  name: gpu.example.com
spec:
  driver: gpu.example.com
  parameters:
    sharing: time-sliced
    memory: 24Gi
    replicas: 4
  nodeSelector:
    accelerator: nvidia
